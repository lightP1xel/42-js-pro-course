import { isValid, sendData, FormHandler } from '../src/functions'

describe('Tests', () => {
  it('', () => {
    expect(true).toBeTruthy();
  })
  // units
  describe('isValid', () => {})
  describe('sendData', () => {})
  // integration
  describe('FormHandler', () => {})
})
